{{>header}}
<div class="container">
    <h3>Actualizar curso</h3>
    {{{respuesta}}}

    <form class="needs-validation" action="/cursos/actualizar" method="POST" novalidate>
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Cursos</label>
            {{{cursosDisponible}}}
        </div>
        <!--<div class="form-group row">
            <label class="col-sm-2 col-form-label">Id</label>
            <div class="col-sm-10">
                <input type="number" class="form-control" placeholder="id del curso" name="id" value="{{curso.id}}"
                    disabled>
            </div>
        </div>
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Nombre</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" placeholder="nombre del curso" name="nombre"
                    value="{{curso.nombre}}" disabled>
            </div>
        </div>
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Descripción</label>
            <div class="col-sm-10">
                <textarea type="text" class="form-control" placeholder="descripción del curso" name="descripcion"
                    value="{{curso.descripcion}}" disabled></textarea>
            </div>
        </div>
        <div class="form-group row ">
            <label class="col-sm-2 col-form-label">Modalidad</label>
            <div class="col-sm-10">
                <select name="modalidad" class="form-control" name="modalidad" value="{{curso.modalidad}}" disabled>
                    <option>Presencial</option>
                    <option>Virtual</option>
                </select>
            </div>
        </div>
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Intensidad</label>
            <div class="col-sm-10">
                <input type="number" class="form-control" placeholder="intensidad del curso" name="intensidad"
                    value="{{curso.intensidad}}" disabled>
            </div>
        </div>
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Valor</label>
            <div class="col-sm-10">
                <input type="number" class="form-control" placeholder="valor del curso" name="valor"
                    value="{{curso.valor}}" disabled>
            </div>
        </div> -->
        <div class="form-group row ">
            <label class="col-sm-2 col-form-label">Estado</label>
            <div class="col-sm-10">
                <select name="estado" class="form-control" name="estado" value="{{curso.estado}}">
                    <option value="" selected disabled hidden>Seleccionar...</option>
                    <option>disponible</option>
                    <option>cerrado</option>
                </select>
            </div>
        </div>
        <div class="form-group row">
            <div class="col-sm-10">
                <button class="btn btn-primary">Actualizar</button>
            </div>
        </div>
    </form>

    <script>
        // Example starter JavaScript for disabling form submissions if there are invalid fields
        (function () {
            'use strict';
            window.addEventListener('load', function () {
                // Fetch all the forms we want to apply custom Bootstrap validation styles to
                var forms = document.getElementsByClassName('needs-validation');
                // Loop over them and prevent submission
                var validation = Array.prototype.filter.call(forms, function (form) {
                    form.addEventListener('submit', function (event) {
                        if (form.checkValidity() === false) {
                            event.preventDefault();
                            event.stopPropagation();
                        }
                        form.classList.add('was-validated');
                    }, false);
                });
            }, false);
        })();
    </script>

    <a href="/cursos">volver</a>
</div>
{{>footer}}